# Engineering conventions

*Adopted 2026-09-22 after a developer's review of the codebase and the
research in [engineering-principles-agent-era.md](engineering-principles-agent-era.md).
Every rule here is paired with the check that enforces it, because a
conventions file improves how an agent finds its way, not how it designs.*

## 1. Types

| Rule | Check |
|---|---|
| A service's public methods take and return **records or domain classes**, never `Map<String, Object>`. | `ops/arch/ratchet.sh`: counts public untyped returns per service; the count may only fall (baseline in `ops/arch/baseline.json`). Pre-commit and edit hook. |
| TM Forum payloads are parsed **once at the wire** into typed DTOs. Polymorphism rides `@type` with Jackson `@JsonTypeInfo` and a `defaultImpl`; unknown fields land in an `extensions` map (`@JsonAnySetter`) so they round-trip. | Mapper per resource; a DTO round-trip test per component. |
| `Characteristic.value` and extension blocks stay open (`Object` / `JsonNode`). That is the edge; nothing behind the mapper touches raw JSON. | Code review; the ratchet catches the leak when it becomes a public return. |
| Domain records are exhaustive: sealed interfaces where a `@type` has finitely many house meanings; `switch` without `default`. | Compiler. |

Migration path: type the **boundary first** (controller ↔ service), then the
services the demos lean on (catalog, ordering, ontology), then ratchet the
rest down arc by arc. Never rewrite a component in one go; every batch ends
with its suites green.

**Worked example: product-catalog (22 Sep).** 35 → 0 public `Map<String, Object>` returns, wire unchanged (TMF620 CTK, suites #119/#131 green).
- One record per resource, verdict or receipt, plain names (`LaunchDecision`, `ReadinessItem`, `CheckProductConfiguration`, `PriceView`); shared wire atoms once (`Money`, `Quantity`, `TimePeriod`, `EntityRef`, `NameValue`).
- Key order = component declaration order, pinned with `@JsonPropertyOrder`; `@JsonInclude(NON_NULL)` only where the map used to leave the key off, per-component where a record mixes both; `@JsonProperty("@type")` for TMF markers.
- Open edge stays open: `@JsonAnyGetter @JsonAnySetter Map<String, Object> extensions` on wire records (unknown fields round-trip), `Object` for characteristic values, `JsonNode` for a polymorphic input (string | ref | list), `List<Map<String, Object>>` for the standard's own open blocks (spec values, terms, price conditions).
- Request bodies are records too (`GovernanceRequest`, `ProductConfigurationRequest`) with `@JsonIgnoreProperties(ignoreUnknown = true)` and an `EMPTY` for an optional body; a lenient `@JsonCreator` keeps old parsing (a bare date in `TimePeriod`).
- A mutable stored blob (`GovernanceState`, the `governance_json` column) is a plain class with public fields plus `extensions`, unwrapped into the view with `@JsonUnwrapped`; a `DtoRoundTripTest` (pure Jackson, no context) pins bytes and order, and runs in seconds.

**Worked example: ontology (22 Sep).** 25 → 0 (plus product-ordering's descriptor), wire unchanged (#125, CSR/Home suites, generated SDK identical). What the registry *declares* stays `JsonNode` (actions, agents, `effects`/`emits` copied into a receipt); what the service *computes* is a record (`Check`, `Verdict`, `ExecuteReceipt`, `CustomerContext`, `Recommendation`, `Explanation`, `ConformanceResult`).
- A response written path by path (execute: refused, filed, done) gets a private mutable `Draft` in the service and one `receipt()` that freezes it; a record enriched in stages (ranking, then decision id) grows by `with`-style copies (`ranked(…)`, `decided(…)`), never by mutation.
- A verdict keeps its domain form (`Boolean ok`) and writes its wire form (`verdict: holds|fails|unknown`) from a `@JsonProperty` getter with `@JsonIgnore` on the component; evaluation context (`resolved`) is `@JsonIgnore`d, so the same record serves the check, the receipt and the MCP result.
- `@JsonUnwrapped` puts a check's keys beside the action name (`ActionCheck`); one `@JsonPropertyOrder` is the union of every path's order and `NON_NULL` leaves off what a path never wrote — where paths disagreed, the common path wins and the deviation is in the report, not the code. Protocol shapes the server owns (MCP tool schema, JSON-RPC envelope) are records too; `Map.of(...)` had been randomising their key order.

**Worked example: intelligence (22 Sep).** 70 → 0, wire unchanged (TMF915 CTK, copilot/CSR/workforce/control-plane suites, snapshot diff of 59 endpoints: 25 byte-identical, 21 values-only, 11 key-order-only where `Map.of` had been random, 2 model-authored).
- A store that keeps the caller's document **verbatim** (TMF915 alarm/rule/contract rows) types it as `ObjectNode`, never a record; the projections the service computes (`AiModelView`, `AiModelContractView`) are records rendered to the same tree (`valueToTree`) so one TMF630 filter/`fields=` mechanic serves both halves.
- What a **model** wrote stays `JsonNode` inside the record (`CopilotReply.proposal`, `forecast`) with `@JsonAnyGetter extensions` for keys the model adds; the parse/normalise code keeps working on trees and converts once at the record boundary. Foreign documents from another component (`ticketById`, `processFlow`, `vocSummary`) are `JsonNode` too.
- A request whose whole body is pasted into a prompt (customer summary, ticket reply, wrap-up) is a `JsonNode` body — an open console document — not a record with fifty optional fields; validation ports as `path(...)` checks.
- A record enriched in stages grows by copies (`PriceSimReportView.saved(id,name)`, `withLines(kept)`, `KnowledgeAnswer.cached(true)`); a `Map<String, Record>` keyed by a dynamic name (`byKind`, `workerTypes`) is a keyed collection, not an untyped return.

**Worked example: billing (22 Sep).** 43 → 0 (raw-map bodies 16 → 0), wire unchanged (37 endpoints snapshotted: 31 byte-identical, 6 key-order-only where `Map.of` had been random; TMF678 CTK). Money is the entity's `BigDecimal` as stored, never re-scaled — `0.00` stays `0.00`, a stored `49.90` stays `49.90`; the `DtoRoundTripTest` pins the scale, and a value read back from a stored JSON blob (credited lines) is parsed as `BigDecimal`, not through a `Double`.
- An event that is the API view plus a few keys (`DunningStepReachedEvent` = case + `step` + `billNo`, `InstallmentPaidEvent` = plan + `billNo` + `paidAmount`) is a record with the view `@JsonUnwrapped` — and the unwrapped component must be named FIRST in `@JsonPropertyOrder`, or Jackson writes the added keys before the view's. Event-only maps (`promiseView`, `letterOf` internals) that no public method returns stay maps: events are a standing rule, and a map that serialises identically is not debt.
- Absent-vs-explicit-null on a PATCH (`customizationId: null` clears the row) cannot ride `Optional` — Jackson gives `Optional.empty()` for an absent creator parameter too — so the field is a `JsonNode`: absent → Java null (leave alone), JSON null → `NullNode` (clear).
- A handler with two honest answers is a sealed interface, not a map with different keys: `BillingRunResult` (`Receipt` | `Busy`), `ChannelConsentResult` (row | `Withdrawn`), `DunningRow` (`DunningCase` | `CollectionCase`), rehearsal rows (`Priced` | `Missing`). The foreign catalog documents the run prices from (`offering`, `price`) became `JsonNode` on the client interface; the run's arithmetic reads `path(...)`/`decimalValue()` and the test mocks build trees with `valueToTree`.

**Worked example: insight (22 Sep).** 50 → 0 (raw-map bodies 15 → 0), wire unchanged (82 endpoints snapshotted on two tenants against a zero noise floor: 69 byte-identical, 13 key-order-only where `Map.of` had been random; suites #social-lead/listening/meta/publish, audience activation/async/native, landing, prospect-reach green; the rest blocked on stopped components, not on insight).
- A **user-authored document inside a typed envelope** stays `JsonNode`: the audience criteria tree (`AudienceView.criteria`, `AudienceRequest.criteria` — a JSON string or an object, both accepted as before), a desk preset's `values`, a desk event's `props`, a connector's `config`, a decision's `candidates`/`context`/`evidence` as the publishing service wrote them. The envelope (`id`, `name`, `population`, `@type`) is typed; the service evaluates the document, never re-shapes it; a parse failure degrades to a `TextNode`, as the map path degraded to the raw string.
- A block that is **absent until it exists and then written whole, nulls included** (a decision's `outcome`/`outcomeValue`/`outcomeAt`) is a nullable nested record `@JsonUnwrapped` in its slot of `@JsonPropertyOrder`: Jackson skips a null unwrapped property and writes every key of a present one — `NON_NULL` on the three fields would have dropped a null `outcomeValue` the ontology's sweeper and the CSR desk read today. A receipt that re-labels a row (`@type: DecisionReceipt` in the row's own slot, then the sentences) is the row `@JsonUnwrapped` FIRST plus `receipt`, built from a `with`-style copy (`asReceipt()`).
- One `@JsonPropertyOrder` per suggestion is the union of every kind's facts (`form, fields, stopField, count, features, opened, actions, people`) with `NON_NULL`, and the one-click fix is a sealed `SuggestedAction` (`Preset` | `Http` with a `JsonNode` body) so `decide()` switches on the kind instead of reading `action.get("kind")`. A per-JVM-random `new LinkedHashMap<>(Map.of(...))` seed is not "pinned" by wrapping — put the keys in order.
- The pure-Jackson `DtoRoundTripTest` caught a real slip on the first run (a factory passing `prospectId` into the `email` slot of a five-string record): positional record constructors are where a typo lands now, so every `of(...)`/`with(...)` factory gets an exact-bytes assertion, not only the wire shapes.

**Worked example: quote (22 Sep).** 41 → 0 (raw-map bodies 18 → 0), wire unchanged (64 endpoints snapshotted against a zero noise floor: 55 byte-identical, 4 key-order-only where `Map.of` had been random, 5 scale-only on stored lines; TMF648/TMF699 have no CTK — cpq-rules, order-contract, pricing-esign, sales, lead-scoring, pipeline-board, social-lead, CDP-engagement suites green; b2b/telesales/opportunity-pipeline blocked on stopped business-console and party-interaction, not on quote).
- A **stored JSON column of lines** (`quote.items`) is a `List<QuoteItem>` record with `extensions`, not a `List<Map>`: rows an older JVM wrote in `Map.of`'s random key order parse into the record and re-write in the pinned order, and money inside the blob comes back as the `BigDecimal` that was stored — so the wire now shows `90.000000` where the `Map` read had shown a `Double`'s `90.0`. Numerically identical, textually not: report it as scale-only, never re-scale to hide it, and pin the write side's scale (`setScale` at computation) only as its own deliberate change.
- A list the map **left off when empty** (a deal's `items`/`activities`) is `@JsonInclude(NON_EMPTY)` on that component alone; a nested block the map left off when *null* (`owner`, `salesOpportunity`) is a nullable record with `NON_NULL` — the two are different rules and one annotation does not serve both.
- The foreign documents a quote is priced from (intent, offerings, price, allowances) and the receipts it gets back (order, agreement) are `JsonNode` on the client; the CDP's one small answer (`LeadSignal`) is a record with a `NONE` sentinel so fail-soft stays typed; what quote *sends* downstream (`ProductOrderRequest`, `AgreementRequest`, `NarrativeContext`) is a record too, so the outbound TMF shapes are pinned by the same test.

**Worked example: usage (22 Sep).** 34 → 0 (raw-map bodies 27 → 0), wire unchanged (125 endpoints snapshotted against a two-run noise floor: 92 byte-identical, 33 key-order-only — `Map.of` had been random, and a stored TMF635 document used to keep its posted key order under the overlay; the 15 remaining diffs are the noise floor's own list growth and DB member order. Suites usage_policy and ocs green; TMF635 2022/2022, TMF677 60/60, TMF654 837/837; journey_* blocked on stopped campaign, configurable_channels on the stopped app, storefront at the appointment slot grid — none on usage).
- A **TMF document stored verbatim and answered with an overlay** (TMF635 usage and specification, the TMF654 task echoes) comes in as `ObjectNode` and goes out as a record with the derived keys declared and the rest in `@JsonAnyGetter extensions` — declared keys first, stored keys after; the map had kept the posted order with overrides in place, so this is the one key-order change the report owns. A tier table whose shape is ours (`Tier`) is a typed list both ways, stored as the same JSON array.
- A **seam's projection is the house record on the adapter interface** (`OcsClient.subscribersOf` → `List<OcsSubscriber>`): the generic adapter parses the stand-in's JSON straight into it (`ignoreUnknown`), the vendor adapter maps its own document (SigScale's TMF637 product, a `JsonNode`) into it; consumers (`PrepayBucketView`, the running-low translation) never see a vendor shape.
- The **internal door and the public machine seam do not share a body record when only one may name the tenant**: `SpendThresholdNotification` (internal, `tenantId` honoured) and `SpendChargeRequest` (gateway-facing, the caller's tenant only) — one record would have let a public caller switch tenant from the body. Absent-vs-null on a cap or a limit rides `JsonNode`, as in billing.
- The snapshot's own writes must be **rows the CTK accepts** (a `reserveBalance` without `relatedParty` failed 12 kit assertions across three runs — two of them written by the old image): take the kit's noise floor before the change too, and delete only what the snapshot itself wrote.

**Worked example: device-commerce (22 Sep).** 31 → 0 (raw-map bodies 7 → 0), wire unchanged (91 endpoints snapshotted against a zero noise floor: 49 byte-identical, 41 key-order-only where `Map.of` had written `relatedParty` at random, 1 value-order-only — a refusal's model list from `Set.of`; suites device_commerce, installments, bnpl_recurring, psp_bnpl_settlement, psp_bnpl_remittance, device_model green; install_slot_rollback and device_entitlement blocked on stopped appointment and device-entitlement, not on device-commerce).
- A **seam's answers are typed on the interface** (`FinancingProvider` → `FinancingQuote`, `EarlySettlementQuote`, `FinancingSettlement`): one record per answer shared by every driver (operator book | mock bank | BNPL), a static factory per model (`writeOff(…)`, `bank(…)`, `delegated(…)`) so each path writes only its own facts, `@JsonPropertyOrder` the union of the paths, `NON_NULL` per component; the service stamps its own keys last by copy (`forAgreement(id, currency)`).
- A **receipt is the row `@JsonUnwrapped` FIRST plus the facts of the act** (`SettleReceipt`, `SwapReceipt`, `WithdrawalReceipt`), and an idempotent replay returns the same record with the facts null — `NON_NULL` leaves them off, so a replay serialises as the bare row it always was; facts that must be written whole with nulls (`WithdrawalReceipt.AgreementFacts`) are a nullable nested `@JsonUnwrapped` record, as in insight.
- **Randomness leaks into values, not only keys**: an estimate's note listing defects from a `Map.of`, a refusal naming models from a `Set.of` — the snapshot flags them structural though no key moved. Put such tables in declaration order (`LinkedHashMap`, `List.of`) and report the sentence change as value-order-only; a body that a record refuses (`HttpMessageNotReadableException`) keeps the 400-with-message contract through the exception handler.

**Worked example: campaign (22 Sep).** 28 → 0 (raw-map bodies 14 → 0), wire unchanged (116 endpoints snapshotted against a two-run noise floor: 85 byte-identical, 22 key-order-only where `Map.of`/`Map.copyOf` had been random, 3 value-order-only — the lifecycle refusal's `Set.of` list, 2 structural = the noise floor's own holdout hash and the tenant's growing portfolio; 22 suites green, 4 blocked on stopped intelligence/process, 2 pre-existing failures the old image fails identically).
- **The decision seam's record is the event AND the answer**: `DecisionRecord.view()` is what `DecisionRecordedEvent` carries (keys the decision log and the ontology's outcome sweep read) and what a dry run answers, re-labelled by a `with`-style copy (`asDryRun()` = `@type: DecisionDryRun`, `decisionId` null under `NON_NULL` on that one component) and `@JsonUnwrapped` FIRST under `dryRun: true`. `Map.copyOf` in `DecisionRequest` had been shuffling the logged `context` per JVM — a copy that lands in a log is `Collections.unmodifiableMap(new LinkedHashMap<>(…))`.
- **A view that is another view re-labelled** (a learning contract = the decision point's keys under `@type: LearningContract`, then `contract`) is the inner record `@JsonUnwrapped` with a `labelled(type)` copy; the two honest shapes of the contract (the tenant's row | the defaults marked as such) are a sealed pair (`ContractView.Stored` | `Defaults`), each with its own `@JsonPropertyOrder`, never one record with half its keys null.
- **A stored JSON ledger of the service's own rows** (the tuning log) is a `List<TuneEntry>` both ways — rows an older image wrote parse into the record (`ignoreUnknown`, `NON_NULL` on the keys only some rows carry: `z`, `threshold`, `decisionId`) and re-write byte-identical; the policy behind the seam still reads the rows as its open context (`convertValue(rows, STEP_LIST)`), so the record boundary is the ledger, not the policy.

**Worked example: party-account + loyalty (22 Sep).** 18 → 0 and 19 → 0 (raw-map bodies 11 → 0 and 3 → 0), wire unchanged (39 + 21 endpoints snapshotted against a zero noise floor: 36 + 15 byte-identical, 3 + 5 key-order-only where `Map.of` had been random; TMF666 1802/1802, TMF669 10153/10153; TMF632 answers at the gateway's `/tmf-api/party/v4/` base — the README's `partyManagement` base has never been routed — with 36 pre-existing unfiltered-list failures the old image fails identically on a 233-organization tenant; family, roles, registry-address green; the rest blocked on stopped components or on data, never on the two services).
- A fan-out answer whose shelves are **foreign documents beside the service's own** (a privacy passport's categories, an erasure report's rows) is one `List<JsonNode>`: the siblings' shelves arrive as trees, the service's own shelves are a record (`CategoryReceipt`, one static factory per shelf, `NON_NULL`) rendered with `valueToTree` — one list, one type, and the record still pins the bytes. A constant table that lands in an answer (`RETAINED`) is a `LinkedHashMap` in declaration order, never `Map.of`.
- A row the service **computes and also stores** (a directory listing) is a record on the way in and a `JsonNode` on the way back: the fresh run answers `List<DirectoryListing>`, a past run read from the column answers the stored rows verbatim (`DirectoryExportRunDetail`), and the run header the outbox event carries is the same record `@JsonUnwrapped` FIRST in both. A nested block a DTO class carried as a map (`organization`, `householdPayer`, `parentOrganization`, `deviceAllowance`) becomes a small shared record (`EntityRef`, `Money`) with `ignoreUnknown`, so a posted `{id, href, name}` still parses.
- Where the map wrote a clock with `.toString()` the record keeps the `String`, and the round-trip test expects what `OffsetDateTime.toString()` writes — `10:00Z`, not `10:00:00Z`. A request record replacing `String.valueOf(map.get(...))` turns the absent key's literal `"null"` lookup (a 404) into the service's own 400 — the contract it always meant; say so in the report. A suite's fallback that reads the service's own top-N journal can be scrolled out by the snapshot's writes: keep the snapshot's rows net-zero and few, and check the ledger before blaming the change.

**Worked example: shopping-cart + user-roles + policy (22 Sep).** 14 → 0, 13 → 0, 12 → 0 (raw-map bodies 4 → 0, 9 → 0, 6 → 0), wire unchanged (63 + 44 + 61 endpoints snapshotted against a zero noise floor: 36 + 38 + 60 byte-identical, 26 + 3 + 0 key-order-only where `Map.of` had been random or a cart line's `quantity` moved before the channel's own keys; TMF663 132/132; agentic-commerce, deals, roles, tenant, operator-form, shadow-clone, prospect-sim, import-base, policy, usage-policy, pricing, price-parity, segment/colour pricing, cpq-rules, collections, guardrails green; storefront, configurable-channels, launch-governance, personalization, privacy, console suites blocked on stopped appointment/business-console/process/intelligence/party-interaction, never on the three).
- A **creator-bound `@JsonAnySetter` reverses the extension keys**: Jackson buffers unknown properties for a record's canonical constructor last-first, so a storefront line posted as `key, offeringId, name, selections` came back `selections, name, offeringId, key`. Where extensions carry more than one key, build the record from the posted map with a delegating creator (`@JsonCreator(mode = DELEGATING) static CartItem fromMap(Map<String, Object>)`) — declared keys pulled out, the rest kept in encounter order — and keep `@JsonAnyGetter` for the write side only. The earlier `EntityRef`s survive because a reference rarely carries two extras; the round-trip test must post several.
- **`String.valueOf(map.get("id"))` on a write path mints the literal**: the id-less onboarding form created a Keycloak realm and a tenant block called `null` (the snapshot proved it on the old image, and had to delete the realm, restore `tenants.yml` and purge the seeded catalog rows). A record's null is refused with the 400 the form always meant; the same slip in `erase` would have deleted every guest cart (owner `IS NULL`) — a nameless erasure is a 400 too.
- A **body that may be the context itself** (`/price/indicative` takes `{context: …}` or the bare variables) is a `JsonNode` body; the typed pair (`DecisionRequest` = `domain` + `JsonNode context`) serves evaluate, price and experience, and the JSON-logic context reaches the engine as the open map it always was. The rule's own `condition` stays the authored text on the wire (a string, as every console posts it); accepting the object form too is the one contract widening, and the snapshot case that exercised it became a **write the old image refused** — a cleanup list keyed on the old run's ids misses it. Delete by the snapshot's own naming, not by what the old image created.
- A **decision with two honest shapes** is a sealed pair on the wire (`DecisionView.Allowed` | `ByRule`, the latter writing a null `message` for a launch envelope), built from the domain record's `view()`; a receipt that grows by one label (`indicative: true`) is a `NON_NULL` last component set by a copy (`asIndicative()`), not a second record. Money read leniently by the map (`10` → `10.0` through a `double`, `"10"` exactly) keeps its `JsonNode` + helper so the create echo does not change scale.

**Worked example: payment + communication + revenue + product-stock (23 Sep).** 11 → 0, 11 → 0, 10 → 0, 9 → 0 (raw-map bodies 4 → 0, 7 → 0, 4 → 0, 3 → 0), wire unchanged (27 + 26 + 25 + 19 endpoints snapshotted against a zero noise floor: 22 + 17 + 25 + 11 byte-identical, 4 + 7 + 0 + 8 key-order-only where `Map.of` had been random, 1 message-only, 0 structural; TMF676 102/168 and TMF681 167/279 at their pre-change counts — both images fail identically — TMF687 0 failures; psp_card_orchestration, psp_klarna, psp_paypal, psp_bnpl_settlement, psp_bnpl_remittance, esp, message_template, marketing_preference, revenue green; configurable_channels and guest blocked on stopped business-console and appointment, never on the four).
- **A `String.valueOf(map.get(...))` can be the only thing guarding a query.** `/payment/confirm` with an empty body looked up the literal `"null"` and 400'd by accident; the record's honest `null` matched `session_ref IS NULL` — which every card payment has — and answered with somebody else's payment. The shopping-cart lesson was the write mirror (a realm named `null`); this is the read mirror, and it is the more dangerous one because nothing fails. Type the boundary and then **re-read every lookup the old literal was accidentally shielding**; the fix is the 400 the endpoint always meant, and the snapshot's refusal cases are what catch it.
- A step that returns **the caller's document copied with a few keys overlaid** is usually not an overlay contract at all: check what the caller reads back. `materialize`/`renderInline` copied the whole send request to override three keys, and `send` read exactly those three — so the pair collapses to `RenderedMessage(subject, content, messageType)` and the request stays a record. Where the overlay *is* the contract (a TMF687 body stored verbatim and answered with the server's keys), the standard's mandatory attributes in which **a posted value wins over the computed default** stay `JsonNode` on the record while the computed ones are typed — and the declared-keys-first order is the key-order change to report, as in usage.
- A send has two honest answers, so `SendOutcome` is a sealed pair whose *first arm is the resource view itself* (`MessageView implements SendOutcome`) — no wrapper, and the 201's `Location` keeps the literal `"null"` the map path minted for a suppressed send, because that is the standing contract.
- A **CSV that must stay byte-identical** survives the rewrite when every `entry.get("k")` becomes `entry.k()` and nothing is re-scaled or re-formatted on the way: `String.valueOf(entry.entryDate())` for the clock, `plain(l.debit())` for the money. All three ERP flavours, the rev-rec timeline and the MRR waterfall came back byte for byte — a subledger whose views were already built in a `LinkedHashMap` has no key-order debt to pay, only `Map.of` receipts.
- **A snapshot must never move money.** A "refund on the wrong state" case ran against captured rows and actually refunded two demo payments before the run was made read-only: take refusal shapes from ids that cannot exist, not from the first row of the list.

**Worked example: qualification (23 Sep).** 8 → 0 (raw-map bodies 8 → 2), wire unchanged (50 endpoints snapshotted against a five-block noise floor of the snapshot's own persisted rows: 32 byte-identical, 13 key-order-only where `Map.of` had been random, 0 structural; TMF645 R18 615/615, TMF679 R18 164/164; servicequal, qualification_gate, fixed_wholesale_console green; wholesale_open_access fails at an upstream-order callback the pre-change image fails identically on).
- **A `HashMap` response has a key order too, and it is not random.** The TMF679 verdict document was built in a plain `HashMap`; Java's `String.hashCode` is specified, so five fixed keys iterate the same way in every JVM and the wire has been stable for years. Do not "fix" it into declaration order — run the five `put`s in a throwaway `java Scratch.java`, read the `keySet()`, and make that the `@JsonPropertyOrder`. The same trick prices the change before you make it: a `HashMap` seeded from the *caller's* keys cannot be reproduced by any record, so that echo stays an open map inside the typed envelope.
- **An `isX()` helper on a record is a wire key.** `CheckItemView.isQualified()` — a verdict question the service asks itself — serialised as `"qualified":true` on every item of every check. Records have no private methods to hide behind: any public no-arg getter-shaped method is a property, so `@JsonIgnore` it. The round-trip test caught this on its first run, before the container was built; a suite would have caught it forty minutes later, and a consumer never.
- A view the service **writes fresh and also reads back from its own column** can be one record with no `extensions` when the column provably holds no other keys — check, do not assume: `select distinct k from t, jsonb_array_elements(col) e, jsonb_object_keys(e) k` answered in a second what a `@JsonAnySetter` would have cost in reversed key order. The same query showed rows written by different JVMs disagreeing on the inner `Map.of` order already, which is what makes pinning them a key-order-only report rather than a regression.
- A request the standard lets arrive in four shapes (place as object or list, under `searchCriteria`, under `service`, or bare) is a record of **open components** — the record's job there is that a body cannot carry a field the service never declared, not that every leaf is typed. Two write faces stay raw maps on purpose: the R18 task resources store the caller's whole document verbatim, and a record would be the one thing that could not.

**Worked example: geographic-address (23 Sep).** 8 → 0 (raw-map bodies 5 → 0), wire unchanged (45 endpoints snapshotted against a zero noise floor: 45 byte-identical, 0 key-order-only, 0 structural — the views were already `LinkedHashMap`s, so there was no `Map.of` debt to pay; TMF674 7/7 requests, 0 failures both sides; geosite and guyana_market fail at a shipping step on the stopped fulfilment component — geosite proved identical on the pre-change image — and registry_address is blocked on the stopped mock-freg, whose `unavailable` answer the snapshot pins byte for byte).
- **`Boolean.FALSE.equals(map.get("enabled"))` is not `Boolean enabled` in a record.** The map path only ever disabled a binding when the caller sent a JSON `false`; the string `"false"` and the number `0` left it enabled. Jackson coerces both into a `Boolean` field, so a record would have silently started disabling registries that used to stay on. Keep the field a `JsonNode` and write the old comparison out (`isBoolean() && !booleanValue()`). Same family as `String.valueOf(map.get(...))`: the map's **leniency** is part of the contract, and only the refusal cases in a snapshot or a round-trip test show it.
- Two honest shapes with **different key orders** are two arms, and the order is the tell: a bound registry answers `provider, country, outcome…`, an unbound one `country, outcome, reason`. One record with `NON_NULL` would have written the bound arm's order for both. Where a nested block is the *sibling's* document (a registry's `registeredAddress`), it stays an open map — the adapter normalises the answer shape, never the vendor's contents.
- A create that is **a validation plus a save** need not carry the validation's open body: the posted address becomes a record, and the static factory that turns it into the wash's submitted block (`AddressValidationRequest.of`) lives on the request record — so the service never grows a `public Map<String, Object> asMap()` for the ratchet to count.

**Worked example: promotion + agreement (23 Sep).** 5 → 0 and 5 → 0 (raw-map bodies 4 → 0 and 3 → 0), wire unchanged (33 + 38 endpoints snapshotted against zero noise floors: 33 + 38 byte-identical, 0 key-order-only, 0 structural; TMF651 12/12 requests and TMF668 10/10, 0 failures both sides; partnership, quote_order_contract, roles green; sla blocked on the stopped assurance component, loyalty fails on bill data the pre-change image fails identically on, guest on the stopped appointment).
- **A refusal that prints a `Set.of` re-writes itself on every JVM start.** Two runs against the *same* container agree, so a one-run noise floor hides it; the new container reshuffled `status must be one of [inProcess, active, terminated]` into `[terminated, active, inProcess]` and the snapshot called it structural. The fix is a `LinkedHashSet` — but pin it to **the order the wire already has**, which need not be declaration order (geographic-address had been printing `active, retired, planned` from a field list declared `planned, active, retired`). Read the order off the old snapshot, not off the source.
- A **caller-echo scale** survives only if the request keeps the map's own leniency: `new BigDecimal(String.valueOf(value))` read a posted `10.00` through a `Double` and stored `10.0`, so the create echo says `10.0` while a re-read from a `numeric(_,2)` column says `10.00`. A `BigDecimal` request field would quietly have made both `10.00`. Keep such a field a `JsonNode` and call `asText()` — it tracks whatever the mapper does with floats, so the record cannot drift from the map.
- Where a body's field is **only ever read leniently** — `instanceof Number` (a posted `"12"` was ignored), `instanceof Map` (a characteristic is legally a map *or* a list), two spellings of one fact (`type`/`agreementType`, `engagedParty`/`engagedPartyRole`) — the component is a `JsonNode` and the resolution lives on the request record (`resolvedType()`, `resolvedParties()`), not in the service. An explicit JSON null must read as *absent* there, because a map's `get` returned Java null for both.
- A **`fields=` projection that used to walk the map** becomes `valueToTree` over the record: the tree carries the record's `@JsonPropertyOrder` and, under `NON_NULL`, is missing exactly the keys the map used to leave out — so `containsKey` → `has` keeps both the projection and its silences.

**Worked example: product-inventory + porting + document + payment-method + recommendation (23 Sep).** 4 → 0, 4 → 0, 4 → 0, 2 → 0, 1 → 0 (raw-map bodies 0 → 0, 1 → 0, 2 → 0, 1 → 0, 0 → 0), wire unchanged (25 + 23 + 24 + 18 + 9 endpoints snapshotted against zero noise floors: 25 + 12 + 24 + 18 + 5 byte-identical, 0 + 11 + 0 + 0 + 4 key-order-only where a two- or three-entry `Map.of` had been re-salted per JVM, 0 structural; TMF637 455/517 with 62 failures at its exact pre-change count — the old image fails identically, twice — bundle_decomposition, family, affinity, object_store, psp_card_orchestration, bnpl_recurring, installments, collections, porting's own assertions green; household, byo_cms, guest, personalization and porting's churn rung blocked on the stopped appointment, mock-sanity, mock-analytics and intelligence).
- **A reference block the caller wrote and the store keeps is a `JsonNode`, not a record.** product-inventory's `productOffering`/`billingAccount`/`previousOffering` go in as posted and come back as stored; an `EntityRef` record with declared `id, href, name` would have re-ordered every caller who posted `name` first. The ratchet counts `Map<String, Object>` returns, and `JsonNode` clears it while keeping the document verbatim — that is the whole point of the open edge. Its patch-time comparison must then read `asText()`, never `String.valueOf(node)`: a `TextNode("5")` and an `IntNode(5)` print differently but were one id to the map.
- **`Map.of` with only two entries is random too.** It is easy to think the salt only bites the big tables; `Map.of("id", …, "role", "customer")` had been writing `{role, id}` in porting and recommendation and `{id, role}` in payment-method, from identical code. Pin every one of them, and read the order to pin from the **old snapshot**, not from the source line.
- A **filter that reads a key the view never wrote** is dead, and typing is when you find out: `banners()` filtered on `!Boolean.FALSE.equals(m.get("available"))` over a map that had no `available` key, so it excluded nothing. Keep the behaviour, say so in a comment, and report it — hiding an unavailable banner is a decision, not a consequence of giving the view a type.
- A **vocabulary table that is printed** shows up in three of these services (`IMAGE_TYPES`, `KNOWN_PROVIDERS`, both `STATUSES`). Sweep for `Set.of` reaching a message before the rebuild, not after: `grep -n 'Set\.of' | grep -v import` over the service, then check each hit for a `+ TABLE` in a refusal.

**Worked example: device-entitlement + base-migration (23 Sep).** 19 → 0 and 14 → 0 (raw-map bodies 2 → 0 and 3 → 0), wire unchanged (77 + 78 endpoints snapshotted: 72 byte-identical against a four-endpoint noise floor of the ECS request log the snapshot itself grows, and 78 byte-identical against a zero floor; 0 key-order-only, 0 scale-only, 0 structural on either. No TM Forum CTK covers either face — device-entitlement is GSMA TS.43/RCC.14 and base-migration is a house API. Suite #124 device_entitlement_test green on all 15 legs, and the ontology's runtime conformance still says device-entitlement and the registry agree; base_migration_test is blocked at its rehearsal simulation on the stopped intelligence component — the pre-change image fails identically at the same POST — so the desk's console leg was proven on its own and the service's six MVC journey tests carry the rest).
- **A protocol with two dialects needs one tree, not two renderers.** The TS.43 answer goes out as JSON *and* as `wap-provisioningdoc` XML, and the XML renderer used to walk the same `Map` the JSON was serialised from. Type the blocks, render once (`valueToTree` at the door), and let the renderer walk the tree: an object becomes a nested characteristic, an array repeats it, a null is left off (`isNull()`, where the map said `v != null`). The record's `@JsonPropertyOrder` then governs both dialects at once — the `parm` order of the XML follows the key order of the JSON because they are the same tree. The prose that reads the answer back (a log line summarising what the phone was told) walks that tree too, and every `String.valueOf(m.get(k))` in it becomes a helper returning the node's own text and the literal `"null"` for a missing key, or the sentence changes.
- **What you cannot observe, you must not pin.** The rule is "read the order off the old snapshot" — so when a `Map.of` is only reachable by an act the snapshot must not perform (SGP.22's `DownloadInfo` exists only if you actually order an eSIM profile), keep the identical `Map.of(...)` expression as a `Map<String, Object>` field inside the record. Pinning it to declaration order would be a change you have no evidence for; keeping the expression is provably byte-identical. Where a random order is observable only through a *side effect* — the TS.43 §2.6 refresh payload is serialised into a notice nobody stores — take that one act deliberately, say so in the report, and pin what it showed.
- **A cast that throws is part of the contract too.** `(Map<String, Object>) raw` on a matrix row answered 500 for a row that was not an object; `(ObjectNode) raw` throws the same `ClassCastException` and keeps the 500. Typing is not the moment to improve a refusal — record the latent fault and leave the wire alone. In the same family: `getOrDefault(k, DEFAULT)` returns the **stored** value when the key is present with a JSON null, so it must read as `has(k)` + the literal `"null"`, never `hasNonNull(k)` — the latter would silently start accepting `{"inBinding": null}` that the door has always refused.
- **A snapshot's read can be a write when a seam answers.** `PUT /subscriber` with an ICCID the operator has never seen looked like a refusal case; the AUC seam *allocated* an IMSI for it and the "refusal" created a subscriber called nothing. Sweep the refusal cases for the ones that reach a seam before you decide they are read-only, delete what the snapshot minted by its own naming, and keep a reset step inside the script for the fields it deliberately writes (here a lenient `featureOverrides`, and an override used to reach the RCS-enabled document at all).

**Worked example: appointment + fulfilment + assurance (23 Sep).** 11 → 0, 6 → 0 and 6 → 0 (raw-map bodies 6 → 0, 4 → 0, 4 → 0), wire unchanged (82 + 53 + 48 endpoints snapshotted against zero noise floors on both images: 80 + 53 + 48 byte-identical, 0 key-order-only — the `Map.of` orders were pinned to what the old snapshot showed, so nothing moved — 0 scale-only, 0 structural; the 2 remaining are the deliberate erasure guard below. TMF642 22/22 requests and TMF656 8/8, 0 failures on both images. appointment_roster, storefront, guest, geosite, shipping_track, partnership, sla green — storefront and geosite had been blocked all week on the stopped appointment and fulfilment and now pass end to end; fulfilment_test and install_slot_rollback and csr_test and privacy_test are blocked on the stopped carrier mocks, the shop's bundle card, trouble-ticket and party-interaction, and the pre-change images fail identically at the same step).
- **A seam's projection is a record only where the house owns the shape.** appointment's `ScheduleProvider` carries the caller's TMF646 blocks (`relatedPlace`, `relatedEntity`, `requestedTimeSlot`) as open nodes, because the house calendar *reads* them and the TMF646 adapter *forwards* them — a record there would re-order every operator's workforce system. fulfilment's `CarrierAdapter.pickupPoints` is the opposite half: a named carrier normalises Bring's document into a `PickupPoint` record and renders it (`valueToTree`), while the generic HTTP adapter passes the operator's own carrier document through — so the interface returns `List<JsonNode>` and the record fills it only where we know the vendor. One rule decides both: type what the house computes, keep a tree for what a sibling wrote.
- **`String.valueOf` on a nested block prints Java, not JSON.** assurance's alarmedObject fell back to `String.valueOf(map)` when the reference carried no id, and the alarm table holds rows reading `{noId=obj-1}` and `[a, list]`. Neither `asText()` (empty) nor `node.toString()` (`{"noId":"obj-1"}`) reproduces that — the faithful mirror is `String.valueOf(mapper.convertValue(node, Object.class))`, which also gives `"7"` for a number and `"null"` for a JSON null in one helper. Put that helper in the service's own `Json` class beside `present`/`set`/`textOrNull` and let every ported `containsKey` / `get(k) != null` / `String.valueOf` read through it; the round-trip test then pins the odd payloads, not just the tidy ones.
- **A derived query turns a record's honest null into `IS NULL`.** appointment's `/privacy/v1/erase` read `String.valueOf(request.get("partyId"))`, so a body with no party looked up the literal `"null"` and deleted nothing by luck. Spring Data renders a null `SIMPLE_PROPERTY` parameter as `IS NULL`, so the typed record would have deleted **every guest booking** — the shopping-cart write mirror and the payment read mirror, now in a third shape. A nameless erasure is the 400 the door always meant; it is the one intended wire change in the batch, and the snapshot's `{}` and `{"partyId":null}` cases are what proved it.
- **A CTK whose assertion total grows with the ledger needs a different invariant.** TMF642 answered 5242/5242 before the change and 5310/5310 after — and 5378/5378 when the *pre-change* image was swapped back in, because every run appends rows the next run's list assertions iterate over (+68 a run here, +100 for TMF656). "The counts must match" means requests (22/22, 8/8), failures (0, 0) and the same per-run step; re-run the old image *after* the new one to separate data growth from a regression, exactly as the snapshot's two-run noise floor does.

**Worked example: trouble-ticket + party-interaction + knowledge (23 Sep).** 5 → 0, 5 → 0 and 3 → 0 (raw-map bodies 3 → 0, 2 → 0, 2 → 0), wire unchanged (75 + 57 + 56 endpoints snapshotted against zero noise floors: 62 + 25 + 56 byte-identical, 6 + 24 key-order-only, 0 scale-only, 0 structural; the 2 + 2 remaining are the erasure guard below and 5 + 6 list headers that move with the ledger, proven by two runs of the *same* image. TMF621 500 → 512 on the old image, 524 on the new and 536 on the old again — requests 14/14 and 0 failures on every run, step +12; TMF683 826 → 906 → 986, requests 18/18, 0 failures, step +80, with the whole 5 500-row history still carrying the mandatory channel/direction/reason trio. interaction_timeline, omnichannel, fixed_wholesale_console green; knowledge_test, knowledge_help_test, csr_test, csr_workspace_test and care_chat_test reach the stopped intelligence and assurance components, social_care_test the stopped mock-social, privacy_test the stopped appointment, and search_test fails on a nine-day-old article whose tags make the keyword search speak where the suite needs it silent — the pre-change image fails identically.)
- **A TMF document answered as an overlay pays one key-order change, and it is worth naming up front.** party-interaction stores the posted body and overlays the server's facts, so `map.putAll(stored)` kept every posted key *in its posted position* with the overrides written in place. A record writes its declared keys first and the stored extras after (`@JsonAnyGetter`), so every row that carries a payload reshuffles — same keys, same values. Where the house derives a block (the mandatory `channel`/`reason` trio, the customer+agent `relatedParty`) it is a record rendered with `valueToTree`; where the caller wrote it, it stays the node they posted — including a channel that is a bare string or a number, and an explicit null, which `NON_NULL` must not eat: on these records `NON_NULL` means "the key was never there", and a posted `null` is a `NullNode` that still prints.
- **A snapshot's write can fan out through the bus into a sibling's store.** Resolving the snapshot's own ticket published a domain event; communication sent a "Ticket resolved" message and party-interaction minted a touchpoint — seven rows each, under the invented party the snapshot had named, in two databases it never called. Sweep the siblings the event reached, by the name the snapshot chose, and take the noise floor again afterwards; the earlier lesson was that a *seam* can answer a read, this one is that the *bus* can answer a write.
- **The pre-change image you swap back in is a new JVM, not the baseline.** trouble-ticket's `organization` block printed `@referredType, id` from the container that had been serving for weeks and `id, @referredType` from the *same image* restarted an hour later — so a snapshot taken after the swap-back would have "proved" a change that never happened, and pinning to it would have moved the live wire. Read the order off the snapshot of the container that is actually serving, pin that, then use the swap-back only for what it is good for: separating a kit's data growth from a regression.
- **A rebuild is when a dormant Flyway conflict detonates.** knowledge's container had been running since before a commit that added `db/migration/V2__customer_situation_articles.sql` beside the existing `db/migration-postgresql/V2__row_level_security.sql` — two files, one shared version space, nine days asleep because nobody rebuilt the component. The first `docker compose build` since put the service in a crash loop on "Found more than one migration with version 2". `mvn clean` is the *other* cause and not this one: check the two folders as a pair, rename to the next free version (the migration was idempotent and applied as V5 without a data change), and remember that an untouched component's next rebuild is where months of unbuilt commits land.

**Worked example: process + flow + event-hub + bss-bridge (23 Sep) — the last batch; the fleet reaches 0.** 3 → 0, 1 → 0, 1 → 0, 1 → 0 (raw-map bodies 2 → 0, 0 → 0, 1 → 0, 1 → 0), wire unchanged (47 + 1 + 26 + 29 faces snapshotted against zero noise floors: **every one byte-identical** — 0 key-order-only, 0 scale-only, 0 structural — because every `Map.of` was pinned to what the running container printed *before* the rebuild). No TM Forum CTK covers TMF701 or TMF688. launch_governance, order_journey, eventhub, audience_snapshot, audience_sql, audience_scheduler, bss_traits, traits_region_org green; process_memory passes its three process rungs and blocks at `/ai/v1/incident`, demo_test plays acts 1–4 and blocks at act 5's `/ai/v1/churnModel`, bss_bridge_test passes its three bridge rungs and blocks at mock-social's activation leg, fulfilment_test at the stopped fulfilment — all four on stopped components, never on the four services.
- **The bus is a wire, and a snapshot must diff it.** bss-bridge answers a receipt and *publishes an envelope*; only the envelope has a consumer that parses it (insight's trait store). Bracket the run with the topic's end offset (`kafka-get-offsets.sh`), then read back exactly the messages the run wrote (`kafka-console-consumer.sh --partition 0 --offset N --max-messages M`) and diff those bytes beside the HTTP answers. It caught the real risk here: `buildOrg` **iterates a `Map.of` INTO the event body**, so the published `organization` block's key order was per-JVM random and a rebuild would have reshuffled it. Pinning a config table to the order the wire already had is not a domain-event change — it is the thing that makes "wire unchanged" checkable at all.
- **An SSE envelope is a contract with no status code to hide behind.** Live Flow's move is read key by key by the page, so `FlowMove` pins `eventId, eventTime, eventType, source, tenant, reactors, ref, keys` and the threading keys are a record with `NON_NULL` (`party, order, intent, quote, object`) whose empty form still serialises as `{}` — a `LinkedHashMap` built by conditional `put`s and a record with `NON_NULL` are the same wire when the puts run in the record's declared order. Read the shape off the live stream first: the replay buffer is empty after a restart, so capture it from the container that is still serving, and prove the new one by running a suite that fires the events you need.
- **A mutating lever needs a row the snapshot owns.** TMF701's `PATCH /taskFlow` and TMF688's delivery ledger cannot be exercised read-only, and patching a demo flow moves a demo. Insert the probe rows by SQL with **fixed ids you chose** (`snapshot-probe-flow`, `snapshot-probe-sub-a`) and a `started_at`/`created_at` far in the past so they never enter a top-N list and never perturb the unfiltered pages; give the task a zero allowance so the stuck sweep skips it; then the lever's success path, its refusals and its ledger rows are all deterministic across runs. Run 1 vs run 2 still differs (run 1 created the spec); the floor is run 2 vs run 3, and that is the baseline to compare the new image against.
- **A registration that matches everything is a live listener the moment it exists.** Three of event-hub's POST cases produce a subscription with no usable filter, which then fans the whole bus into the delivery ledger and starts POSTing to a dead port. Order such cases **last** in the script and delete them by the callback the snapshot named within seconds — the same discipline as "a read can be a write when a seam answers", one step further out.
- **`List.of(...).contains(x)` throws on a null.** `String.valueOf(map.get("state"))` handed the allowed-states check the literal `"null"` and got the 400 the door meant; a typed `String state` hands it a Java null and `ImmutableCollections.indexOf` throws NPE — a 500 where a 400 had always been. Keep the component a `JsonNode` and read it through the service's own `Json.valueOf`, which is the one helper this batch put in all four services: `absent(node)` (a missing key and an explicit JSON null were one thing to `get`), `valueOf(node)` (`String.valueOf(convertValue(node, Object.class))` — `"7"`, `"{a=1}"`, `"[a, list]"`, `"null"`), `textOrNull(node)`.

## 2. Modules and size

| Rule | Check |
|---|---|
| A Java class does one thing and stays under ~400 lines; a service package has a `controller`, `service`, `mapper`, `dto`, `entity`, `repository` split. | Review; ArchUnit rules where a component has them. |
| A front-end file stays under **300 lines**; a React component renders one concern. | `ops/arch/ratchet.sh`: front-end file line counts may only fall from baseline. |
| No file grows past what an agent can read in one pass; split before adding. | Same ratchet. |

## 3. Data, events, tenancy

| Rule | Check |
|---|---|
| Every table carries `tenant_id`; every table gets a row-level-security migration in `db/migration-postgresql`. | `PostgresMigrationTest` per component; RLS suite. |
| Flyway versions are **shared** between `db/migration` and `db/migration-postgresql`. | Flyway refuses duplicates; `mvn clean` first. |
| Every state change publishes a domain event through the **outbox** in the same transaction; consumers are idempotent. | Suite per arc asserts the event; the martech sweep after any event change. |
| No cross-component database access. A component reads another only through its API or its events. | ArchUnit `noClasses().dependOnClassesThat().resideInPackage("..other.entity..")` where present; review. |

## 4. Channels and UI

| Rule | Check |
|---|---|
| Channels ask, never compute: pricing from the catalog's configurator, eligibility from policy, actions from the ontology. | Suite #131 pattern; review. |
| Every front end sends `X-Channel`; every agent surface sends `X-GenAlpha-Agent`. | Gateway refuses what a channel cannot see; suite #118. |
| React for channels; **no new vanilla-JS files**. The vanilla consoles are migrated desk by desk: split into files first (classic scripts in load order, so the shared globals keep working), then mount React islands with `createRoot` into the existing ids, one desk at a time, deleting the old desk when the new one is green. | Ratchet on file size; suite per desk. |
| Screens speak operator language: names, never keys or UUIDs; healthy states stay quiet; every page has a goal line and a ? drawer. | Screenshot + look before handover; `console_home_test` asserts no UUID text on Home. A no-UUID assertion on every console page is a follow-up. |
| Accessibility: keyboard operable, visible focus, colour never the only signal. | Suite spot checks. |

## 5. AI

| Rule | Check |
|---|---|
| Copilots propose a card; a human presses Create. Agents act only through registered ontology actions with `check` and `execute` under the caller's token, and every act writes a receipt. | Suite #125; the decision log. |
| Every model call is metered and logged in the AI ledger; a stub provider runs every suite without a key. | AI audit page; suites run with `AI_PROVIDER=stub`. |
| Prompts and contracts live in code with the component, versioned; no prompt in a front end. | Review. |

## 6. Seams and vendors

| Rule | Check |
|---|---|
| Anything vendor- or country-specific sits behind a seam: one interface, one adapter per vendor, a stand-in in the fleet, selection per tenant in `tenants.yml`. | Every seam has a `mock-*` container and a suite that runs against it. |
| Fail soft on a seam: a quiet vendor never blocks the customer's action; the gap is logged and reconciled later. | Suite asserts the action completes with the stand-in down where that is the contract. |

## 7. Documents and proof

| Rule | Check |
|---|---|
| Every arc has `docs/<arc>.md` ending in **Honest limits**. | Review at arc end. A `--docs` check on the ratchet is a follow-up; older docs use varying headings. |
| A feature exists when its numbered suite is green through the gateway with a real token. | `ops/run-all-suites.sh`. |
| README rows, the capability map and the architecture diagrams follow every shipped component. | Review at arc end. |
| Decisions that lock a design get an ADR in `docs/adr/`. | Review. |

## 9. Identifiers that may be missing

`String.valueOf(map.get("id"))` on a value that is absent produces the four
characters `null`, not an absence. Today the fleet has ~370 such reads, and
they **fail closed**: a lookup for the id `"null"` matches nothing, so the
caller gets a 404 or an empty list.

The danger is the *next* change. When one of these is typed honestly, the
missing value becomes a real `null`, and a derived query turns that into
`WHERE column IS NULL` — which matches every row that never had one. That is
not theory: the typing arc introduced and caught it six times, and two of
those would have been serious. Confirming a payment with an empty body
returned another customer's payment; an erase request with no party would
have deleted every guest booking, and in another service 149 ownerless
tickets. Two more minted a tenant and a realm literally named `null`.

| Rule | Check |
|---|---|
| A request body's identifier is **declared on a record** and a missing one is **refused explicitly** (400), never defaulted, never stringified. | `ops/arch/ratchet.sh` counts `String.valueOf(x.get("…id…"))` per service; the count may only fall. |
| An identifier read out of a foreign document is read null-safely (`node.isNull()` first) and stored as an absence, not as text. | Review; the same ratchet count. |
| Never hand a possibly-null identifier to a derived query. If it can be absent, branch before the query. | Review. The failure is silent: the query succeeds and matches the wrong rows. |

The ratchet number is therefore a **ceiling on future risk**, not a count of
present bugs. It exists so that the next person who types one of these is
forced to decide what a missing identifier means.

## 8. What we deliberately do not do

- No business logic in front ends, no shared database, no free-form pricing formulas, no prompt-only guardrails.
- No rewrite arcs. Debt is paid down inside feature arcs under the ratchet.
- No "regenerate instead of maintain" for anything with a suite: regeneration is a regression surface, the suite is the contract.
