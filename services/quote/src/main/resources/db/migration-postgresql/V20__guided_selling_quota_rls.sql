GRANT SELECT, INSERT, UPDATE, DELETE ON guided_question, guided_recommendation, sales_quota TO quote_app;

ALTER TABLE guided_question ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON guided_question
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE guided_recommendation ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON guided_recommendation
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE sales_quota ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON sales_quota
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));
