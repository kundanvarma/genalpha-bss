-- Same tenant wall as every SOM table.
ALTER TABLE dealer_agreement ENABLE ROW LEVEL SECURITY;
ALTER TABLE starter_kit ENABLE ROW LEVEL SECURITY;
ALTER TABLE commission_entry ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON dealer_agreement
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));
CREATE POLICY tenant_isolation ON starter_kit
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));
CREATE POLICY tenant_isolation ON commission_entry
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));
