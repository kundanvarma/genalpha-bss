-- The second lock for the new scheduling tables: same policy shape as V3.
ALTER TABLE schedule_config ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON schedule_config
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE technician ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON technician
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));
