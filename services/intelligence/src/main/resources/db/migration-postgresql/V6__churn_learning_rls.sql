ALTER TABLE churn_feature_snapshot ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON churn_feature_snapshot
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE churn_outcome ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON churn_outcome
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));

ALTER TABLE churn_model ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON churn_model
    USING (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true))
    WITH CHECK (current_setting('app.tenant_id', true) = '__system__'
           OR tenant_id = current_setting('app.tenant_id', true));
