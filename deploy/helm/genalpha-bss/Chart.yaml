apiVersion: v2
name: genalpha-bss
description: >-
  TM Forum ODA BSS: five Spring Boot services (catalog, ordering, inventory,
  party-account, gateway) with optional in-cluster PostgreSQL, Kafka, and
  Keycloak for local/dev clusters. On cloud, disable the local dependencies
  and point the values at managed services.
type: application
version: 1.0.0
appVersion: "1.0.0"
