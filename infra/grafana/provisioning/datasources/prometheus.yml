apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # pinned so the provisioned dashboard's panels bind to it by uid
    uid: bss-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
